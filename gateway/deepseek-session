#!/usr/bin/env bash
# Full interactive Claude Code session running on DeepSeek.
# Scoped to this terminal: other Claude sessions are unaffected.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec node "$SCRIPT_DIR/deepseek-session.mjs" "$@"
