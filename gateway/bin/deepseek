#!/usr/bin/env bash
# Global shortcut: "deepseek" instead of the full path to deepseek-session.
# Installed in ~/.local/bin (already in PATH, alongside claude).
exec node "$HOME/.claude/deepseek-gateway/deepseek-session.mjs" "$@"
